var x = 1;
// pring out all even numbers 
while ( x < 11){
    if (x % 2  === 0){ 
        console.log ("x is currently set to: "+x);
    }
    x = x + 1;
}

X = 0;
while (x < 5) {
    console.log ("x is currently set to: "+x);
    x = x + 1;
    if (x === 2){
        console.log ("x is set to 2 break: "+x);
        break
    }
}
